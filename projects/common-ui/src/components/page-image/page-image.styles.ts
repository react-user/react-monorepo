import { createStyles, makeStyles } from "@material-ui/core";

export const pageImageStyles = makeStyles(
    createStyles({
        text: {
            fontSize: "80px",
            fontWeight: "normal",
            lineHeight: "89px",
        },
    })
);
